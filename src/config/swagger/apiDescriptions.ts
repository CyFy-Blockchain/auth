export const ApiDescriptions = {
  CREATE_DEPT:
    'This API is responsible to create a new department in the DB. One department can have multiple members in them. One department can have multiple admins associated with them.',
  CREATE_ORG:
    'This API is responsible to create a new organisation in the DB. One organisation can have multiple departments in it. One organisation can have multiple organisations in it.',
};
